sudo: required

language: node_js

services:
  - docker

before_script:
  - echo "#define WIFI \"$MY_WIFI\"" > wifipass.h
  - echo "#define PASS \"$MY_PASS\"" >> wifipass.h
  - cat wifipass.h

#script:
#  - docker-compose build
#  - docker-compose up

#before_deploy:
#  - git config --local user.name $USER_NAME
#  - git config --local user.email $USER_EMAIL
#  - git tag "$(date +'%Y%m%d%H%M%S')-$(git log --format=%h -1)"

#deploy:
#  provider: releases
#  api_key: $GHTOKEN
#  file: "bin/tom-cube.bin"
#  skip_cleanup: true
